import React from 'react';
import classes from './Card.css';

const Card = () => {
    return (
        <div className = {classes.Card}>
            <p>X-ray image</p>
        </div>
    );
};

export default Card;